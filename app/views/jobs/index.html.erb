All Jobs for <strong><%= @company.name %></strong>

<% @jobs.each do |job| %>
  <ul>
    <a href="/companies/<%= @company.id %>/jobs/<%= job.id %>"><%= job.title %></a> at <%= job.company.name %>
  </ul>
<% end %>
