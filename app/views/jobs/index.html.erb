<%= stylesheet_link_tag "index_show" %>
<header>
  <h3>All <%= @jobs.company.name %></strong> Jobs</h3>
</header>

<div class="all-jobs-list">
  <% @jobs.each do |job| %>
    <ul>
      <a href="/companies/<%= @company.id %>/jobs/<%= job.id %>"><%= job.title %></a> at <%= job.company.name %>
    </ul>
  <% end %>
</div>
