<%= stylesheet_link_tag "create_edit" %>
<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <div class="buttons-container">
    <%= f.submit %>
    <a class="buttons-container-1" href="/companies">Cancel</a>
  </div>

<% end %>
